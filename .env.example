# =============================================================================
# SANITY CMS CONFIGURATION
# =============================================================================
# These variables are required for Sanity CMS integration

# Project ID from your Sanity project dashboard
# Get this from: https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID="your-project-id"

# Dataset name (usually "production" for live site)
NEXT_PUBLIC_SANITY_DATASET="production"

# API version in YYYY-MM-DD format
# Use current date or check Sanity docs for latest
NEXT_PUBLIC_SANITY_API_VERSION="2025-08-15"

# URL for the Sanity Studio (admin interface)
# For local development: http://localhost:3000/studio
# For production: https://yourdomain.com/studio
NEXT_PUBLIC_SANITY_STUDIO_URL="http://localhost:3000/studio"

# =============================================================================
# SANITY TOKENS (Server-side only)
# =============================================================================

# Viewer token for reading data (get from Sanity project settings > API)
# This token should have "Viewer" permissions
SANITY_VIEWER_TOKEN="your-viewer-token"

# Secret for preview/draft mode
# Use a secure random string (keep this secret!)
SANITY_PREVIEW_SECRET="your-preview-secret"

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local
# 2. Replace all "your-*" values with your actual Sanity project credentials
# 3. Never commit .env.local to version control
# 4. For production, set these as environment variables in your hosting platform